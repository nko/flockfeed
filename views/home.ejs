<h1>Welcome, <%= current_user.name %>!</h1>

<p>Below you can find the link to your personal Twitter RSS feed. Simply copy and paste the URL (or if you're using a browser like Firefox, just click through to the link and you can add it from the screen there).</p>

<a id='rss_link' href='/feeds/<%= current_user.key %>'>http://flockfeeds.com/feeds/<%= current_user.key %></a>